// Basic configuration
export const CANVAS_SIZE = 480         // pixels (square canvas)
export const TILE = 20                 // size of one tile (px)
export const INITIAL_SPEED = 120       // ms per tick (lower -> faster)

